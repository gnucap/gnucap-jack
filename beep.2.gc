load public gnucap-default-plugins.so
* load public libjack.so
* attach ./d_meter.so
* attach ./s_jack.so
* attach ./bm_jack.so
* attach ./bm_jack_wrap.so
attach ./gnucap-jack.so

options trace

verilog

to_jack #(.connect(1)) outjackL(0 1);
to_jack #(.connect(2)) outjackR(0 1);

spice
V1 0 1 sin frequency=440 amplitude=1

.print tran v(1) v(2)
.nodelist
.jack trace=a
.end
