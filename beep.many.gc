load public gnucap-default-plugins.so
load public libjack.so
attach ./d_meter.so
attach ./s_jack.so
attach ./bm_jack.so
attach ./bm_jack_wrap.so

options trace

verilog

to_jack #(.connect(1)) outjack(0 1);
param f=440

spice
V1 0 1 sin frequency=f amplitude=1


.print tran v(1) v(2)
.nodelist
.jack stop=1 trace=a
.param f=880;
.jack stop=.5 trace=a
.param f=440;
.jack stop=.5 trace=a
.end
